spring:
  application:
    name: weaver
  datasource:
    url: jdbc:postgresql://localhost:5432/weaver
    username: postgres
    password: sondang2906
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8091